# Spectrum credentials (from your project Settings on the Photon dashboard — https://app.photon.codes).
# Required for the iMessage provider. Omit both to run terminal-only (no creds needed).
PROJECT_ID=
PROJECT_SECRET=

# Sawa web app base URL. Required for the read commands (/markets, /search, /show, /suggest).
# Reads hit the app's PUBLIC GET endpoints (${SAWA_API_BASE_URL}/api/predictions*) — no DB key,
# no JWT (the private-market guard is enforced server-side). See docs/BETTING_BOT_PLAN.md §6 (Option C).
SAWA_API_BASE_URL=https://sawapredictions.com

# Optional: template for tappable market links, e.g. https://sawapredictions.com/market/{id}
# SAWA_MARKET_URL_TEMPLATE=

# pmxt — cross-venue (Kalshi/Polymarket) read-only enrichment API (https://pmxt.io).
# Free tier: 25,000 credits/month (1 REST call = 1 credit; 1 WS message = 0.1 credit).
# Rate limits: 60 req/min, 5 concurrent WS streams.
# Betting/trading stays on Sawa — pmxt is read-only enrichment, never the trade host.
PMXT_API_KEY=
# Builder mode: when true, a fee is charged on trades placed through the integration.
PMXT_BUILDER_MODE=true
