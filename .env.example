# Spectrum credentials (from your project Settings on the Photon dashboard — https://app.photon.codes).
# Required for the iMessage provider. Omit both to run terminal-only (no creds needed).
PROJECT_ID=
PROJECT_SECRET=

# Sawa database (Supabase PostgREST). Required for the read commands (/markets, /search, /show, /suggest).
# Use the *publishable* key, never a service-role key. See docs/DATABASE_MAP.md.
SAWA_SUPABASE_URL=
SAWA_SUPABASE_KEY=

# Optional: template for tappable market links, e.g. https://sawapredictions.com/market/{id}
# SAWA_MARKET_URL_TEMPLATE=
